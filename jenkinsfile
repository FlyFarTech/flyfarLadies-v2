pipeline {
    agent any
    stages {
        stage('Build and Deploy') {
            steps {
                // SSH into the remote server using SSH Agent
                sshagent(['api_ladies']) {
                    // Run docker-compose up command to build and start Docker containers
                    sh 'ssh root@192.241.145.76 "cd /www/wwwroot/2ndvolume/api.flyfarladies.com && bash flyfar_ladies.sh"'
                }
            }
        }
    }
}
